// Single source of truth for the app version, baked into the JS bundle.
// Bump this together with `expo.version` in app.json on every release — the
// in-app updater compares it against the latest GitHub release's version.json.
// Made in Italy.
export const APP_VERSION = '1.0.0';

// The user's own GitHub repo is the ONLY update source (no external servers).
export const UPDATE_REPO = 'blackstardigitalstudio/blackstar-player';
